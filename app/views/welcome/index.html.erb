<% if @top_organization %>
  <banner>
    </br>
      <h3>
        <%= @top_organization.name %>
      </h3>
      <%= @top_organization.description %>
      <%= link_to "Help", organization_path(@top_organization) %>
    </br>
  </banner>

  <content>
    <ul class="top_organizations">
      <% @top_orgs.each do |organization| %>
        <li>
          <%= organization.name %>:
          <%= organization.description %>
        </li>
      <% end %>
    </ul>
  </content>
<% end %>